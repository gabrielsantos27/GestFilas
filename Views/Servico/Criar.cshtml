@model Servico

@{
    ViewData["Title"] = "Novo Serviço";
}


<form asp-action="Criar" method="post" class="d-flex justify-content-center">
    <div class="card card-primary" style="background-color: white; color: black; width: 50rem;">
        <div class="card-header">
            <h3 class="card-title">@ViewData["Title"]</h3>
        </div>
        <!-- /.card-header -->
        <div class="card-body">
            <div class="form-group">
                <label asp-for="NomeServico">Nome do Serviço</label>
                <input type="text" asp-for="NomeServico" class="form-control form-control-border" id="exampleInputBorder" placeholder="">
                <span asp-validation-for="NomeServico" class="text-danger"></span>
            </div>
            <div class="form-group mt-3">
                <div class="d-flex justify-content-end">
                    <input type="submit" value="Guardar" class="btn btn-success me-2 mr-2" />
                    <a asp-action="Listagem" class="btn btn-info">Retornar</a>
                </div>
            </div>
        </div>
    </div>
</form>
